<%= form_with(model: event_request, local: true) do |form| %>
  <% if event_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_request.errors.count, "error") %> prohibited this event request from being saved:</h2>

      <ul>
      <% event_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recurring_event_id %>
    <%= form.text_field :recurring_event_id %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
